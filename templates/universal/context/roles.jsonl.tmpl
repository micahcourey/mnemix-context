// Role definitions for {{project.name}}
// Format: one JSON object per line
// Fields: role, description, category, external_role, notes
// Populate via @Setup-Wizard agent or manually from your auth system
//
// Example:
// {"role": "ADMIN", "description": "System Administrator", "category": "internal", "external_role": "admin-user", "notes": "Full system access"}
// {"role": "READER", "description": "Read-only User", "category": "external", "external_role": "app-reader", "notes": "View-only access"}
