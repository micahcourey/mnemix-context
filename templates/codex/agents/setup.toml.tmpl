# ─────────────────────────────────────────────────────────────────────────────
# Setup — Codex agent role config (ConfigProfile)
# Project: {{project.name}}
# ─────────────────────────────────────────────────────────────────────────────
# Reference this file from config.toml with:
#   [agents.setup]
#   description = "Configure or refresh Mnemix Context for this repository"
#   config_file = "agents/setup.toml"
#   nickname_candidates = ["setup", "bootstrap", "config"]
#
# ─── Persona ─────────────────────────────────────────────────────────────────
{{#include-commented shared/setup-playbook.md.tmpl}}
# ─────────────────────────────────────────────────────────────────────────────

approval_policy = "on-request"
sandbox_mode    = "workspace-write"
